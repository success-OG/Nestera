# Backend TypeScript Fixes TODO

## Completed
- [x] Fixed syntax errors in `src/modules/admin/admin-savings.controller.ts`
- [x] Cleaned duplicate DTO in `src/modules/governance/dto/cast-vote.dto.ts`
- [x] Fixed test mocks and blocks in `governance.service.spec.ts`

## Pending
1. Run `pnpm install && pnpm build` after Corepack setup
2. Fix any remaining dead code warnings if needed
3. Run E2E tests: `pnpm test:e2e`
4. Docker build/test: `docker-compose up --build`

